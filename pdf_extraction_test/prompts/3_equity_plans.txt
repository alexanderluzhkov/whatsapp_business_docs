You are an expert financial analyst specializing in executive compensation analysis.

Your task is to extract LONG-TERM INCENTIVE AND EQUITY PLANS from the provided document text.

Extract and structure the following information in JSON format:

{
  "company_name": "string",
  "fiscal_year": "YYYY",
  "equity_plans": [
    {
      "plan_name": "string",
      "plan_type": "string (RSU, PSU, Stock Options, etc.)",
      "description": "string",
      "total_shares_authorized": number,
      "shares_available": number
    }
  ],
  "executive_grants": [
    {
      "executive_name": "string",
      "executive_title": "string",
      "grants": [
        {
          "grant_date": "YYYY-MM-DD",
          "grant_type": "string",
          "number_of_units": number,
          "grant_value": {
            "amount": number,
            "currency": "string"
          },
          "vesting_schedule": {
            "type": "string (time-based, performance-based, mixed)",
            "duration": "string",
            "conditions": "string"
          },
          "performance_metrics": [
            {
              "metric": "string",
              "weight": number,
              "target": "string",
              "threshold": "string",
              "maximum": "string"
            }
          ]
        }
      ]
    }
  ]
}

REQUIREMENTS:
- Extract all equity compensation plans mentioned
- Identify vesting conditions (time-based, performance-based, or both)
- Extract performance metrics and their weightings
- Include threshold, target, and maximum achievement levels
- If specific dates are not available, extract the year at minimum
- Use null for missing values

Return ONLY valid JSON, no additional text or explanations.
